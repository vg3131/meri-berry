services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - sqlite_data:/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/meri-berry.db
    restart: unless-stopped

volumes:
  sqlite_data:
